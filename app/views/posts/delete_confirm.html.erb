<h4><%= @post.title %></h4>
<h1>Are you sure you want to delete this article?</h1>

<h2>
  You cannot undo this action, perhaps you just want to
  <%= link_to 'edit', edit_post_path(@post.slug) %> instead?
</h2>

<%= link_to 'Delete', post_path(@post.slug), method: :delete, class: 'button' %>